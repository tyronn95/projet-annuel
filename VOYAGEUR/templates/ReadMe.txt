Pour utiliser la fonctionalité de calcul de distance il faut:
	-installer python (pour avoir la commande python et pip)
	-éxécuter: 
		-pip install Flask
		-pip install -U googlemaps
		-app.py (ce placer la ou le fichier ce trouve avec la commande 'cd')

il ne reste plus qu'a mettre dans la barre de recherche http://127.0.0.1:5000/